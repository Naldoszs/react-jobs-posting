import DotLoader from "react-spinners/DotLoader "; // Default export

const Spinner = ({ loading, size, color }) => {
  return <DotLoader color={color} loading={loading} size={size} />;
};

export default Spinner;
d;
